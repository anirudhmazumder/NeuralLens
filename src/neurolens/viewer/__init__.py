"""Demo viewer.

Serves a single-page UI (`index.html`) that reads a run's `summary.json` and
per-iteration screenshots to show before/after comparison, a schematic brain
heatmap, region bars, the reward trajectory, and the iteration log.

Entry point: `neurolens view <run_dir>` (see cli.py).
"""
